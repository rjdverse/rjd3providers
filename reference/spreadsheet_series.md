# Title

Title

## Usage

``` r
spreadsheet_series(
  file,
  sheet = 1,
  series = 1,
  period = 0,
  aggregation = c("None", "Sum", "Average", "First", "Last", "Max", "Min"),
  partialAggregation = FALSE,
  cleanMissings = TRUE,
  fullName = TRUE
)
```

## Arguments

- fullName:

## Examples

``` r
```
