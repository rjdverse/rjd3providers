# Set the paths of the provider

Set the paths of the provider

## Usage

``` r
set_xml_paths(paths)
```

## Arguments

- paths:

## Examples

``` r
set_xml_paths(system.file("examples", package = "rjd3providers"))
```
